<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Blog - {% block title %}{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>

<nav class="py-2 border-bottom menu-burger">
    <div class="container d-flex flex-wrap">
        <div class="dropdown ">
            <a class="nav nav-link dropdown-toggle text-white" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Mon Blog
            </a>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdminDashboard">
                <li>
                    <a class="dropdown-item" href="{{('admin')}}">Tableau de bord</a>
                </li>
                <li>
                    <a class="dropdown-item" href="#">Menu</a>
                </li>
            </ul>
        </div>

        <div class="dropdown ">
            <a class="nav nav-link dropdown-toggle text-white" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Ajouter
            </a>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdminAdd">
                <li>
                    <a class="dropdown-item" href="#">Article</a>
                </li>
                <li>
                    <a class="dropdown-item" href="#">page</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <div class="d-flex align-content-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
            <a class="fs-4 text-dark" href="{{ path('app_accueil') }}">Progressive Code</a>
        </div>

        <ul class="nav nav-pills">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path('app_logout')}}">Se Déconnecter</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{path('app_register')}}">S'inscrire</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path('app_login')}}">Se Connecter</a>
                </li>
            {% endif %}
        </ul>
    </header>
</div>

{% block body %}{% endblock %}

<div class="container">
    <footer class="d-flex flex-wrap justify-content-between align-align-items-center py-3 my-4 border-top">
        <div class="col-md-4 d-flex align-items-center">
            <span class="copyright">&copy; {{ 'now' | date('Y') }} Pentiminax. Tous droits réservés.</span>
        </div>
    </footer>
</div>

</body>
</html>
