{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center">Tous les articles de la catégorie {{ categorySlug }}</h1>

        {% for article in articles %}
            <div class="p-4 mb-3 bg-light article">
                <h4 class="fst-italic">{{ article.title }}</h4>
                <p>{{ article.content }}</p>
            </div>
        {% endfor %}
    </div>
{% endblock %}
